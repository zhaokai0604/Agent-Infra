<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.award.log.mapper.AlarmRuleMapper">

    <resultMap id="AlarmRuleResultMap" type="com.award.log.model.AlarmRuleEntity">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="description" property="description" />
        <result column="rule_type" property="ruleType" />
        <result column="rule_expression" property="ruleExpression" />
        <result column="severity" property="severity" />
        <result column="push_channels" property="pushChannels" />
        <result column="enabled" property="enabled" />
        <result column="create_by" property="createBy" />
        <result column="update_by" property="updateBy" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <insert id="insert" parameterType="com.award.log.model.AlarmRuleEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO alarm_rule (
            name, description, rule_type, rule_expression, severity, push_channels, enabled, create_by, update_by
        ) VALUES (
            #{name}, #{description}, #{ruleType}, #{ruleExpression}, #{severity}, #{pushChannels}, #{enabled}, #{createBy}, #{updateBy}
        )
    </insert>

    <update id="update" parameterType="com.award.log.model.AlarmRuleEntity">
        UPDATE alarm_rule
        SET name = #{name},
            description = #{description},
            rule_type = #{ruleType},
            rule_expression = #{ruleExpression},
            severity = #{severity},
            push_channels = #{pushChannels},
            enabled = #{enabled},
            update_by = #{updateBy},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM alarm_rule WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="AlarmRuleResultMap">
        SELECT * FROM alarm_rule WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="AlarmRuleResultMap">
        SELECT * FROM alarm_rule ORDER BY id DESC
    </select>

    <select id="selectEnabled" resultMap="AlarmRuleResultMap">
        SELECT * FROM alarm_rule WHERE enabled = 1 ORDER BY id DESC
    </select>

    <update id="updateEnabled">
        UPDATE alarm_rule
        SET enabled = #{enabled},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
