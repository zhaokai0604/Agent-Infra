# ThreshCore 部署指南（新机 Jar + Dist）

> 银河麒麟 V11 · LoongArch64 · 单端口 8088 · 2026-07-05 实测  
> 面向**全新目标机**：只部署预构建产物，不在目标机上编译、不跑本地开发环境。

---

## 1. 架构与端口

```
浏览器 → Nginx :80（对外唯一入口）
           ├── /              → /opt/threshcore/dist/（Vue 静态页）
           └── /award-log/    → 127.0.0.1:8088/award-log/（Jar API）
                                    └── MariaDB log_analysis
```

### 1.1 单端口 8088

当前平台为**单端口模式**：

| 项 | 说明 |
|----|------|
| 业务 + 登录 + MCP + Agent | 全部走 **8088**，Context Path `/award-log` |
| 8089 管理端口 | **不启动**（`APP_MANAGEMENT_ENABLED=false`） |
| 用户访问 | 仅 **Nginx :80**（或 443），**不对外暴露 8088** |
| Spring Profile | `prod-kylin`（`prod` + `kylin`） |

环境变量里必须写：

```bash
SPRING_PROFILES_ACTIVE=prod-kylin
APP_MANAGEMENT_ENABLED=false
```

启动 Jar 时建议再加：`-Dapp.management.enabled=false`

验证只有 8088 在监听：

```bash
ss -tlnp | grep -E '8088|8089'
# 应只有 8088；无 8089
```

浏览器 Network 里 API 请求应为 `http://<IP>/award-log/...`，**不能出现 :8089**（若出现说明 dist 过旧，需重新 build）。

---

## 2. 交付物

拷到目标机 `~/下载/`（或任意目录）：

| 文件 | 说明 |
|------|------|
| `award-log-1.0-SNAPSHOT.jar` | 后端 Fat Jar，**必须在 LoongArch 本机构建** |
| `dist/` | 前端静态资源，可在 Windows `npm run build` 后拷贝 |
| `schema.sql` | 数据库建表（来自 `src/main/resources/schema.sql`） |

目标机**不需要**：Maven、Node.js、Docker、Git 仓库。

---

## 3. 目标机软件依赖

```bash
uname -m          # loongarch64
cat /etc/os-release
java -version     # JDK 17
mariadb --version
nginx -v
```

安装（麒麟示例）：

```bash
sudo yum install -y java-17-openjdk mariadb-server nginx
sudo systemctl enable --now mariadb nginx
```

---

## 4. 目录、系统用户与平台账号

部署前分清三类「用户/目录」，避免和文档旧版 `setup_kylin_host.sh` 混淆。

### 4.1 必须创建的目录（`/opt` 平台文件夹）

```bash
sudo mkdir -p /opt/threshcore/logs
sudo cp ~/下载/award-log-1.0-SNAPSHOT.jar /opt/threshcore/award-log.jar
sudo cp -r ~/下载/dist /opt/threshcore/dist
sudo mkdir -p /etc/threshcore
```

| 路径 | 用途 | 是否必须 |
|------|------|----------|
| `/opt/threshcore/` | 平台安装根目录 | 是 |
| `/opt/threshcore/award-log.jar` | 后端 Jar | 是 |
| `/opt/threshcore/dist/` | 前端静态资源（Nginx root） | 是 |
| `/opt/threshcore/logs/` | 应用运行日志 | 是（mkdir 即可，启动后自动写） |
| `/etc/threshcore/threshcore.env` | 环境变量 | 是 |

若下载目录名带空格：`sudo cp -r ~/下载/"dist " /opt/threshcore/dist`

**`/opt/threshcore/tmp` 不必手动建**——当前配置下非必须。

### 4.2 Linux 系统用户 `award-agent`

仓库脚本 `setup_kylin_host.sh` 还会创建：

- 系统用户 **`award-agent`**
- 目录 **`/opt/threshcore/tmp`**
- **`/etc/sudoers.d/award-agent`**

这些用于 **`AGENT_MIN_PRIVILEGE=true`**（Agent 降权 + sudo 执行）。

**当前推荐配置：**

```bash
AGENT_MIN_PRIVILEGE=true
AGENT_RUN_AS_USER=award-agent
```

此时 systemd 以普通运行用户启动，Agent 通过 `sudoers` 放行必要命令，便于和生产部署口径保持一致。

仅当你明确选择不启用最小权限模式时才考虑：

```bash
sudo useradd --system --home-dir /home/award-agent --create-home --shell /sbin/nologin award-agent
sudo mkdir -p /opt/threshcore/tmp && sudo chown award-agent:award-agent /opt/threshcore/tmp
# 安装 scripts/deploy/sudoers.award-agent.example → /etc/sudoers.d/award-agent
# env 中 AGENT_MIN_PRIVILEGE=true
```

### 4.3 MariaDB 数据库

```bash
sudo mariadb -uroot -p -e "CREATE DATABASE IF NOT EXISTS log_analysis DEFAULT CHARSET utf8mb4;"
sudo mariadb -uroot -p log_analysis < ~/下载/schema.sql
```

`schema.sql` **只建表，不插入任何平台登录账号**。

### 4.4 平台登录用户（部署后必做）

与 Linux 系统用户无关，需在**服务启动后**注册：

```bash
curl -X POST http://127.0.0.1/award-log/admin/user/register \
  -H 'Content-Type: application/json' \
  -H 'X-Requested-With: XMLHttpRequest' \
  -d '{"username":"12345678","password":"12345678","email":"12345678@local"}'
```

自注册默认为普通用户。需要管理员（用户管理等）：

```bash
sudo mariadb -uroot -p log_analysis \
  -e "UPDATE sys_user SET role=1 WHERE username='12345678';"
```

---

## 5. 部署步骤总览

| 步骤 | 内容 | 必须 |
|------|------|------|
| 1 | 安装 JDK 17、MariaDB、Nginx | 是 |
| 2 | 建库并导入 `schema.sql` | 是 |
| 3 | 创建 `/opt/threshcore/` 并拷贝 jar + dist | 是 |
| 4 | 写入 `/etc/threshcore/threshcore.env` | 是 |
| 5 | systemd 启动 Jar（或前台 java -jar 调试） | 是 |
| 6 | 配置 Nginx 反代 | 是 |
| 7 | 注册平台账号 + 验收 | 是 |
| — | 创建 Linux 用户 `award-agent` | **是**（推荐配置） |

---

## 6. 环境变量

```bash
sudo tee /etc/threshcore/threshcore.env > /dev/null <<'EOF'
SPRING_PROFILES_ACTIVE=prod-kylin
APP_MANAGEMENT_ENABLED=false

DB_DRIVER=org.mariadb.jdbc.Driver
DB_URL=jdbc:mariadb://127.0.0.1:3306/log_analysis?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
DB_USERNAME=root
DB_PASSWORD=你的MariaDB密码

AI_BASE_URL=https://api.deepseek.com
AI_API_KEY=你的AI密钥
AI_CHAT_MODEL=deepseek-chat

APP_CONFIG_SECRET=随机32位以上字符串
AGENT_RUN_AS_USER=award-agent
AGENT_MIN_PRIVILEGE=true
RF_FORCE_HEURISTIC_LOONGARCH=true

# 浏览器 / Nginx 正式入口 Origin 白名单，多个来源用英文逗号分隔
APP_CORS_ALLOWED_ORIGIN_PATTERNS=https://threshcore.example.com

AWARD_MIDDLEWARE_KAFKA=false
AWARD_MIDDLEWARE_REDIS=false
ELASTIC_ENABLED=false

OPS_DRY_RUN_GLOBAL=false
OPS_AUTO_REMEDIATION_ENABLED=true
OPS_PATROL_ENABLED=true
EOF
sudo chmod 600 /etc/threshcore/threshcore.env
```

**启动前必须加载**，否则 Profile 回落为 `dev`、界面误显示「开发环境」：

```bash
set -a && source /etc/threshcore/threshcore.env && set +a
```

---

## 7. 启动后端

### 7.1 生产：systemd 开机自启（推荐）

```bash
sudo tee /etc/systemd/system/award-log.service > /dev/null <<'EOF'
[Unit]
Description=ThreshCore award-log
After=network.target mariadb.service
Wants=mariadb.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/threshcore
EnvironmentFile=-/etc/threshcore/threshcore.env
Environment=APP_MANAGEMENT_ENABLED=false
ExecStart=/usr/bin/java -Dapp.management.enabled=false -Xms256m -Xmx1024m -jar /opt/threshcore/award-log.jar
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable --now award-log
sudo journalctl -u award-log -f
```

### 7.2 调试：前台 java -jar（看实时日志）

```bash
cd /opt/threshcore
set -a && source /etc/threshcore/threshcore.env && set +a
java -Dapp.management.enabled=false -Xms256m -Xmx1024m -jar award-log.jar
```

关终端或 `Ctrl+C` 即停；验收完成后改回 systemd。

### 7.3 重部署前清理旧进程

```bash
sudo systemctl stop award-log 2>/dev/null || true
sudo pkill -f award-log.jar 2>/dev/null || true
ss -tlnp | grep 8088 || echo "8088 已释放"
```

---

## 8. Nginx

```bash
sudo tee /etc/nginx/conf.d/threshcore.conf > /dev/null <<'EOF'
server {
    listen 80;
    server_name _;
    root /opt/threshcore/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /award-log/ {
        proxy_pass http://127.0.0.1:8088/award-log/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 600s;
    }

    location /award-log/ws/ {
        proxy_pass http://127.0.0.1:8088/award-log/ws/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 3600s;
    }
}
EOF

sudo nginx -t && sudo systemctl reload nginx
```

---

## 9. 验收

```bash
# Jar 健康
curl -s http://127.0.0.1:8088/award-log/actuator/health

# 经 Nginx（用户实际访问路径）
curl -s http://127.0.0.1/award-log/api/platform/acceptance

# 注册 + 登录（见 4.4）
curl -X POST http://127.0.0.1/award-log/admin/user/login \
  -H 'Content-Type: application/json' \
  -H 'X-Requested-With: XMLHttpRequest' \
  -d '{"username":"12345678","password":"12345678"}'
```

浏览器无痕打开 `http://<服务器IP>/` 登录。

通过标准：

- `{"status":"UP"}`
- `deliveryTargetMatch: true`
- 右上角环境为 **生产环境**（非「开发环境」）
- 仅 **8088** 监听，无 **8089**

---

## 10. 升级

```bash
sudo cp ~/下载/award-log-1.0-SNAPSHOT.jar /opt/threshcore/award-log.jar
sudo rm -rf /opt/threshcore/dist && sudo cp -r ~/下载/dist /opt/threshcore/dist
sudo systemctl restart award-log
sudo systemctl reload nginx
```

浏览器 **Ctrl+Shift+R** 强刷。

---

## 11. 构建机（不在目标机执行）

| 产物 | 构建位置 |
|------|----------|
| `award-log.jar` | 龙芯本机：`bash scripts/deploy/deploy_kylin_loongarch.sh` |
| `dist/` | Windows 或龙芯：`cd frontend && npm run build` |

Windows 构建 dist 可选（IP 换成实际值）：

```bash
# frontend/.env.production
VITE_AWARD_LOG_BASE_URL=http://<目标机IP>/award-log
VITE_AWARD_LOG_MANAGEMENT_BASE_URL=http://<目标机IP>/award-log
```

打包 **jar + dist + schema.sql** 传到目标机即可。

---

## 12. 常见问题

| 现象 | 原因 | 处理 |
|------|------|------|
| 右上角「开发环境」 | 未加载 `threshcore.env` | `source` 后再启；systemd 配 `EnvironmentFile` |
| 登录「用户名或密码错误」 | 未注册平台账号 | 先 `/admin/user/register` |
| 页面能开、API 502 | Jar 未起 | `journalctl -u award-log -n 50` |
| API 连 `:8089` | dist 过旧 | 重新 build 并覆盖 `/opt/threshcore/dist` |
| `deliveryTargetMatch: false` | Profile 或系统不对 | 确认 `prod-kylin`、麒麟龙芯 |
| Jar 无法运行 | x86 编译的 Jar | 在 LoongArch 重新构建 |
| 要不要建 `award-agent` | 文档旧流程 | **推荐建；`AGENT_MIN_PRIVILEGE=true` 时必须建** |

---

## 13. 相关文件

| 文件 | 用途 |
|------|------|
| `src/main/resources/schema.sql` | 数据库初始化 |
| `scripts/deploy/deploy_kylin_loongarch.sh` | 龙芯构建 |
| `scripts/deploy/award-log.service` | systemd 参考 |
| `scripts/deploy/nginx.kylin.sample.conf` | Nginx 参考 |
| `scripts/deploy/setup_kylin_host.sh` | 含 MariaDB + award-agent（**可选**，非 Jar+Dist 最小路径） |
| `scripts/deploy/env.kylin.prod.example` | 环境变量模板 |
