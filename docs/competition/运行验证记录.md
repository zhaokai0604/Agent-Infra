# 运行验证记录

> 验证日期：2026-08-16（Asia/Shanghai）
> 验证范围：参赛仓库的后端构建/测试与前端生产构建

## 1. 验证环境

| 项目 | 实际版本 |
|---|---|
| 操作系统 | Windows 11 x64 |
| JDK | Oracle JDK 17.0.12 LTS |
| Maven | 3.9.12 |
| Node.js | 22.20.0 |
| npm | 10.9.3 |

## 2. 后端验证

在仓库根目录执行：

```powershell
mvn verify -DforkCount=0
```

结果：

| 检查项 | 结果 |
|---|---|
| Java 编译 | 通过（424 个主源码文件） |
| 测试编译 | 通过（196 个测试源码文件） |
| 单元/集成测试 | 739 项测试：0 失败、0 错误、1 跳过 |
| 打包 | 生成 `target/award-log-1.0-SNAPSHOT.jar` |
| 覆盖率报告 | 生成 JaCoCo HTML 报告 |

说明：在当前 Windows 中文路径工作区中，Surefire 分叉 JVM 会出现 Manifest classpath 兼容提示；使用 `-DforkCount=0` 后，测试完整执行并通过。该参数只影响测试进程的分叉方式，不改变业务运行产物。

## 3. 前端验证

在 `frontend/` 目录执行：

```powershell
npm ci
npm run build
```

结果：Vite 4.5.14 成功完成生产构建，转换 2742 个模块并生成 `dist/`。构建仅报告大包体积建议；npm 审计报告 13 项上游依赖漏洞（5 moderate、8 high），本次未执行自动升级，以避免非必要的兼容性变更。

## 4. 可复现样例

- 请求：[clean-temp-dry-run.request.json](../../examples/clean-temp-dry-run.request.json)
- 预期回执：[clean-temp-dry-run.response.example.json](../../examples/clean-temp-dry-run.response.example.json)
- 执行方法：[examples/README.md](../../examples/README.md)

该样例是 Dry Run，不访问生产资产、不执行删除，也不包含真实 `traceId`、路径、密钥或用户数据。

## 5. 启动前置条件与边界

测试使用 H2 和测试配置，因此不依赖完整中间件。要启动实际服务，仍需按根目录 README 提供：

1. MySQL 8 或 MariaDB 10.11+ 配置；
2. `application-local.yml` 或环境变量中的本地数据库与模型配置；
3. 可选组件（Qdrant、Elasticsearch、Kafka、Redis）按使用的功能启用。

真实密钥、本地数据库、运行日志、`target/`、`frontend/dist/` 和 `frontend/node_modules/` 均未提交到仓库。
